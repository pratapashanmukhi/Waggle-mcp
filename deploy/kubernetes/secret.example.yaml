apiVersion: v1
kind: Secret
metadata:
  name: graph-memory-secret
type: Opaque
# ============================================================
# ⚠  EXAMPLE ONLY — DO NOT COMMIT REAL CREDENTIALS TO GIT ⚠
# ============================================================
# For production, use external-secret.example.yaml with the
# External Secrets Operator instead of a static secret file.
#
# If you must use a static secret (dev / staging only):
#   1. Copy this file to secret.yaml
#   2. Replace the placeholder values below with real values
#   3. Add secret.yaml to .gitignore BEFORE applying
#   4. Apply: kubectl apply -f secret.yaml
# ============================================================
stringData:
  GRAPH_MEMORY_NEO4J_USERNAME: "neo4j"
  GRAPH_MEMORY_NEO4J_PASSWORD: "REPLACE_WITH_REAL_PASSWORD"
